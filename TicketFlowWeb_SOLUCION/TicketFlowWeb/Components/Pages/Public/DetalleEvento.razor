@page "/public/detalle-evento/{Id:int}"
@using TicketFlowWeb.Services.EventoRS
@inject EventoRestService EventoService
<div class="detalle-evento-page">
    @if (eventoReal == null)
    {
        <div class="text-center py-5">
            <div class="spinner-border text-light" role="status"></div>
            <p class="mt-2">Cargando detalles del evento...</p>
        </div>
    }
    else
    {
        <div class="detalle-grid-principal">

            <div class="columna-portada">
                <img src="@eventoReal.img" alt="Portada del Evento" class="img-portada" />
            </div>

            <div class="columnas-info-cabecera">
                <div class="grid-cabecera-datos">
                    <div class="bloque-cabecera">
                        <label class="etiqueta-mini">CATEGORÍA</label>
                        <h2>@(eventoReal.categoria?.nombre ?? "Sin Categoría")</h2>
                    </div>
                    <div class="bloque-cabecera">
                        <label class="etiqueta-mini">EVENTO</label>
                        <h1>@eventoReal.titulo</h1>
                    </div>
                    <div class="bloque-cabecera">
                        <label class="etiqueta-mini">PRECIO</label>
                        <h2 class="texto-precio">S/. @eventoReal.precio.ToString("F2")</h2>
                    </div>
                    <br />
                    <div class="bloque-cabecera">
                        <div class="descripcion-caja">
                            @eventoReal.descripcion
                        </div>
                    </div>
                    <br />
                    <div class="acciones-nuevas">
                        <a href="@($"/cliente/compra/{Id}")" class="btn-comprar">Comprar entrada</a>
                    </div>
                    
                </div>
            </div>


            <div class="columna-vacia-izq">
                <div class="recuadro-anfitrion">
                    <div class="anfitrion-titulo">ORGANIZADO POR</div>
                    <h3>@eventoReal.anfitrion.nombre</h3>
                    @if (!string.IsNullOrEmpty(razonSocialAnfitrion))
                    {
                        <p class="mb-0 text-white-50">@eventoReal.anfitrion.razonSocial</p>
                    }
                </div>
            </div>

            <div class="columna-datos-tiempo">
                <div class="datos-evento-lista">
                    <div class="fila-dato-nuevo">
                        <label>Entradas Restantes</label>
                        <span>@eventoReal.capacidad_entradas</span>
                    </div>
                    <div class="fila-dato-nuevo">
                        <label>Fecha</label>
                        <span>@(eventoReal.fechaModerna?.ToString("dd/MM/yyyy") ?? "---")</span>
                    </div>
                    <div class="fila-dato-nuevo">
                        <label>Hora</label>
                        <span>@eventoReal.horaInicioModerna - @eventoReal.horaFinModerna</span>
                    </div>

                    <div class="acciones-nuevas">
                        <a href="/public/descubrir" class="btn-volver">Volver</a>
                        <a href="@($"/cliente/compra/{Id}")" class="btn-comprar">Comprar entrada</a>
                    </div>

                </div>
            </div>

            <div class="columna-datos-ubicacion">
                <div class="datos-evento-lista">
                    <div class="fila-dato-nuevo">
                        <label>Local</label>
                        <span>@eventoReal.nombre_establecimiento</span>
                    </div>
                    <div class="fila-dato-nuevo">
                        <label>Ubicación</label>
                        <span>@eventoReal.ubicacion</span>
                    </div>
                    <div class="fila-dato-nuevo">
                        <label>Lugar</label>
                        <span>@(eventoReal.distrito?.nombre ?? "---") - @(eventoReal.distrito?.region?.nombre ?? "---")</span>
                    </div>
                </div>
                @*
                <div class="recuadro-anfitrion">
                    <div class="anfitrion-titulo">ORGANIZADO POR</div>
                    <h3>@nombreAnfitrion</h3>
                    @if (!string.IsNullOrEmpty(razonSocialAnfitrion))
                    {
                        <p class="mb-0 text-white-50">@razonSocialAnfitrion</p>
                    }
                </div>
                *@
            </div>

        </div>
    }
</div>

<style>
    .detalle-evento-page {
        min-height: 100vh;
        background: linear-gradient(90deg, #05051f 0%, #1d1270 45%, #3928d8 100%);
        font-family: Arial, Helvetica, sans-serif;
        color: white;
        padding: 50px;
    }

    /* 🟢 REJILLA EN 3 COLUMNAS IGUALES */
    .detalle-grid-principal {
        display: grid;
        grid-template-columns: 1fr 1fr 1fr;
        gap: 40px;
        max-width: 1300px;
        margin: 0 auto;
    }

    /* Columna 1: Imagen */
    .columna-portada {
        grid-column: 1;
    }

    .img-portada {
        width: 100%;
        height: 500px;
        object-fit: cover;
        border: 4px solid white;
        border-radius: 15px;
        background-color: #222; /* 👈 Esto evita que el contenedor colapse si Google te bloquea */
    }

    /* Columnas 2 y 3 unificadas para la Cabecera */
    .columnas-info-cabecera {
        grid-column: 2 / span 2;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .grid-cabecera-datos {
        display: flex;
        flex-direction: column;
        gap: 15px;
    }

    .etiqueta-mini {
        font-size: 12px;
        letter-spacing: 2px;
        color: #00ffcc;
        font-weight: bold;
        display: block;
        margin-bottom: 2px;
    }

    .columnas-info-cabecera h1 {
        font-size: 48px;
        font-weight: bold;
        margin: 0;
    }

    .columnas-info-cabecera h2 {
        font-size: 28px;
        margin: 0;
    }

    .texto-precio {
        color: #ffcc00;
        font-weight: bold;
    }

    /* Fila de Descripción completa (Abarca de la col 1 a la 3) */
    .columna-descripcion-completa {
        grid-column: 1 / span 3;
    }

    .descripcion-caja {
        border: 3px solid white;
        border-radius: 20px;
        padding: 25px;
        font-size: 20px;
        line-height: 1.5;
        background: rgba(255, 255, 255, 0.05);
    }

    /* Fila Inferior */
    .columna-vacia-izq {
        grid-column: 1;
    }

    .columna-datos-tiempo {
        grid-column: 2;
    }

    .columna-datos-ubicacion {
        grid-column: 3;
    }

    .datos-evento-lista {
        display: flex;
        flex-direction: column;
        gap: 15px;
        margin-bottom: 20px;
    }

    .fila-dato-nuevo {
        display: flex;
        flex-direction: column;
        gap: 5px;
    }

        .fila-dato-nuevo label {
            font-size: 14px;
            font-weight: bold;
            text-transform: uppercase;
            color: #a5a2e6;
        }

        .fila-dato-nuevo span {
            border: 2px solid white;
            border-radius: 8px;
            padding: 10px 15px;
            font-size: 17px;
            background: rgba(0, 0, 0, 0.2);
        }

    /* 🌟 RECUADRO DEL ANFITRIÓN */
    .recuadro-anfitrion {
        border: 3px dashed #00ffcc;
        border-radius: 12px;
        padding: 18px;
        background: rgba(0, 255, 204, 0.05);
        margin-top: 15px;
        margin-bottom: 25px;
    }

    .anfitrion-titulo {
        font-size: 11px;
        letter-spacing: 1.5px;
        color: #00ffcc;
        font-weight: bold;
        margin-bottom: 5px;
    }

    .recuadro-anfitrion h3 {
        margin: 0;
        font-size: 20px;
        font-weight: bold;
    }

    .acciones-nuevas {
        display: flex;
        gap: 15px;
        margin-top: 20px;
    }

    .btn-volver, .btn-comprar {
        flex: 1;
        text-align: center;
        text-decoration: none;
        border-radius: 25px;
        padding: 12px 20px;
        font-size: 16px;
        font-weight: bold;
    }

    .btn-volver {
        color: white;
        border: 3px solid white;
    }

    .btn-comprar {
        color: #160b62;
        background-color: white;
        border: 3px solid white;
    }

    .btn-volver:hover {
        background: white;
        color: #160b62;
    }

    .btn-comprar:hover {
        background: #dffcff;
    }

    /* Responsivo para móviles */
    @@media (max-width: 900px) {
        .detalle-grid-principal {
            grid-template-columns: 1fr;
        }

        .columna-portada, .columnas-info-cabecera, .columna-descripcion-completa,
        .columna-datos-tiempo, .columna-datos-ubicacion {
            grid-column: 1 / span 1;
        }

        .columna-vacia-izq {
            display: none;
        }

        .img-portada {
            height: 350px;
        }
    }
</style>

@code {
    [Parameter]
    public int Id { get; set; }

    private Models.EventoViewModel eventoReal;

    private string nombreAnfitrion = "Cargando...";
    private string razonSocialAnfitrion = "";

    protected override async Task OnInitializedAsync()
    {
        eventoReal = await EventoService.verDetalleEventoAsync(Id);

    }
}